spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=SET TIME ZONE 'UTC'
    username: sa
    password:
    type: com.zaxxer.hikari.HikariDataSource
    jpa:
      show-sql: true